# Appliance layout bundles

Production scripts live in the repository root:

- `scripts/build-foldops-bundles` — build `layout-tar-zst` bundles and schema v2 manifest
- `scripts/publish-foldops-bundles` — upload to `packages.folding-os.com`

See [packages/foldops/README.md](../README.md).
